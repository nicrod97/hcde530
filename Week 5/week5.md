## Week 5 Competency Claim

In `class activity.ipynb`, I used pandas to answer a specific question: how ratings are distributed and how app categories differ in average review outcomes. I loaded `app_reviews_demo.csv` (500 rows, 10 columns), used `value_counts()` on `rating`, and found the distribution is heavily positive (4 stars = 160 reviews, 5 stars = 207 reviews, or 73.4% combined). I then used boolean filtering (`rating <= 2` and `helpful_votes >= 20`) to isolate high-signal negative feedback (38 rows), and `groupby().agg()` to compare categories, where `research repository` had the highest average rating (4.12) and `field research` the lowest (3.67). I also checked data quality with `isna().sum()` and `isna().mean()`, which showed 165 rows with at least one missing value. These results matter because they show both sentiment skew and category-level differences while also flagging missing data as a limitation I should account for in follow-up analysis.